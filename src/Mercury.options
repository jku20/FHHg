# Default target for mmc --make
MAIN_TARGET = fhhg

# Default grade. You can override this in the environment.
GRADE = hlc.gc
MCFLAGS += --grade $(GRADE)

# Statically link to Mercury libraries.
MCFLAGS-hpny += --mercury-linkage static
